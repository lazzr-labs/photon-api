services:
  api-service:
    build: .
    image: ghcr.io/lazzr-labs/photon:ute1787929769
    volumes:
      - .:/code
    ports:
      - "8080:8080"
    command: >
      sh -c "go run api-go --env=docker --server=api"
    networks:
      docker-network: {}

networks:
  docker-network:
    external: true
